# 跨任务类型“是否调工具”共享神经元实验方案

## 0. 一句话思想

把 When2Tool 的三类任务 A/B/C 当成 Safety Kernel 里的“多语言维度”：
先在每一类任务内部，用 `tool_necessary=1` 和 `tool_necessary=0` 的平行对比数据，按 Safety Kernel 的 SCAR 分数找单类型“应该调工具”神经元；再把 A/B/C 三类神经元取交集，得到跨任务类型共享的工具调用决策核心；最后用 Safety Kernel 的 masked LoRA 方式只训练这批共享神经元，并用 When2Tool 指标评测。

这里的目标不是复现 When2Tool 的 Probe&Prefill，而是回答一个更机制化的问题：

> 模型内部是否存在一小批同时影响 A/B/C 三类任务“是否需要调用工具”的共享 FFN 神经元？

## 1. 方法映射

| Safety Kernel | 本实验 |
|---|---|
| 语言 `l` | 任务类型 `c in {A,B,C}` |
| unsafe prompt set `D_l^u` | tool-necessary 样本 `D_{m,c}^1` |
| safe prompt set `D_l^s` | tool-unnecessary 样本 `D_{m,c}^0` |
| language-specific safety neurons | 单类型工具调用决策神经元 `TDN_{m,c}` |
| Shared Safety Core / Safety Kernel | 跨任务类型共享工具调用决策神经元 `CTD_m` |
| ASR | When2Tool 的 `Acc / TC / AvgTC / TCR / Cost`，再补充工具决策诊断指标 |
| SK-Masked LoRA | CTD-Masked LoRA |

## 2. 模型、数据和任务类型

每个模型单独跑完整流程，因为 `tool_necessary` 标签是模型相关的：同一道题对大模型可能不需要工具，对小模型可能需要工具。

| 模型 | When2Tool alias | Hugging Face 路径 |
|---|---|---|
| Qwen3-1.7B | `qwen3-1.7b` | `Qwen/Qwen3-1.7B` |
| Qwen3-4B-Instruct | `qwen3-4b-instruct` | `Qwen/Qwen3-4B-Instruct-2507` |
| Qwen3-14B | `qwen3-14b` | `Qwen/Qwen3-14B` |
| Qwen3-32B | `qwen3-32b` | `Qwen/Qwen3-32B` |
| Llama-3.1-8B-Instruct | `llama3.1-8b` | `meta-llama/Llama-3.1-8B-Instruct` |
| Llama-3.3-70B-Instruct | `llama3.3-70b` | `meta-llama/Llama-3.3-70B-Instruct` |

单跳和多跳严格分开做：

| 子集 | Train | Test | 用途 |
|---|---:|---:|---|
| single-hop | 900 | 2250 | 单跳发现、训练、评测 |
| multi-hop | 180 | 450 | 多跳发现、训练、评测 |

When2Tool 的 A/B/C 映射如下。

| 类型 | 含义 | single-hop 环境 | multi-hop 环境 |
|---|---|---|---|
| A | Can I compute this? 计算规模 | `CalculatorEnv`, `StatisticsEnv`, `CountingEnv`, `MatrixEnv`, `PrimeEnv` | chained calculator |
| B | Do I know this? 知识边界 | `RetrieverEnv`, `HistoricalYearEnv`, `GameRuleEnv`, `HashEnv`, `DecodingEnv` | chained retriever |
| C | Can I execute this reliably? 执行可靠性 | `ListManipulationEnv`, `DateTimeEnv`, `CodeExecutorEnv`, `ScheduleEnv`, `RegexMatchEnv` | chained code executor |

multi-hop 数据在原始 JSON 里仍复用单跳 env 名，例如 chained calculator 的环境名仍是 `CalculatorEnv`，但 `multi_step=True` 且 `expected.steps` 长度为 3。划分 A/B/C 时用数据子集和 env 名共同判断。

## 3. 提示词和运行设置对齐 When2Tool

这一节只回答一件事：When2Tool 开源代码怎么做，我们这个实验就怎么对齐。关键点是 Llama 不走 XML，而是走 native tool calling。

### 3.1 When2Tool 开源依据

开源代码里的工具格式判断是按模型名自动分流：

- `detect_tool_format(model_name_or_path)` 中，只要模型名包含 `llama`，返回 `native`。
- `native` 的含义是 bare JSON tool call，工具返回写成 `role="tool"`。
- 非 Llama 默认 `xml`，也就是 Qwen 用 `<tool_call>...</tool_call>`，工具返回写成 `<tool_response>...</tool_response>` 并作为 user message 追加。

对应依据：

| 结论 | When2Tool 开源位置 |
|---|---|
| Qwen/XML 与 Llama/native 的分流 | `when2tool_repo/src/utils.py:73-86`, `detect_tool_format` |
| XML 系统提示包含 `<tool_call>...</tool_call>` | `when2tool_repo/src/utils.py:53-60`, `SYSTEM_PROMPT_XML` |
| native 系统提示不要求 `<tool_call>` 标签 | `when2tool_repo/src/utils.py:62-67`, `SYSTEM_PROMPT_NATIVE` |
| `hard_no_tool` 是内部标签模式，不是普通评测模式 | `when2tool_repo/src/utils.py:95-97`, `LABEL_MODES=("hard_no_tool",)` |
| `hard_no_tool` 实际先复用 `no_tool` prompt，再在 state machine 里拒绝工具调用 | `when2tool_repo/src/utils.py:112-114` 和 `utils.py:523-532` |
| native 工具返回使用 `role="tool"` | `when2tool_repo/src/utils.py:572-576`, `step_state` |
| 标签阶段用 vLLM，且 `enable_thinking=False` | `when2tool_repo/src/extract_features.py:92-100` 和 `extract_features.py:128-135` |
| 标签阶段生成长度、交互轮数和上下文长度按 `extract_features.py` 默认值 | `when2tool_repo/src/extract_features.py:325-328`，即 `max_new_tokens=2048`、`max_rounds=12`、`max_model_len=32768` |
| 抽激活阶段 re-encode prompt 时 `enable_thinking=False` | `when2tool_repo/src/extract_features.py:176-180` |
| 后续评测自动检测 tool format | `when2tool_repo/src/run_eval.py:471-473` 和 `src/run_probe_eval.py:168-169` |

所以 Llama 的设置要写清楚：它不是 `<tool_call>{...}</tool_call>`，而是 tokenizer/chat template 的 native function calling；模型输出 bare JSON tool call，工具结果作为 `role=tool` 消息回填。

注意一个实现细节：When2Tool 的 `AgentModel` 会按模型自动选择 system prompt；但 `extract_features.py` 里 re-encode hidden prompt 的示例代码直接导入默认 `SYSTEM_PROMPT`，默认是 XML。为了让抽激活和后续评测严格一致，本实验抽激活阶段也显式使用：

$$
system\_prompt_m=get\_system\_prompt(detect\_tool\_format(m))
$$

也就是 Qwen 用 XML system prompt，Llama 用 native system prompt。

### 3.2 固定运行口径

- 所有 Qwen3 和 Llama 阶段都关 thinking：`enable_thinking=false`。
- 主实验用 `no_reasoning`，因为 When2Tool 的 probe/hidden-state 主结果就是 no-reasoning pre-generation signal。
- 跑标签阶段的生成与交互参数按 When2Tool `extract_features.py`：`--max-new-tokens 2048`、`--max-rounds 12`、`--max-model-len 32768`。这三项属于实验口径，默认不因模型或机器改变。
- `--tensor-parallel-size` 是硬件并行参数，不作为方法变量。When2Tool 脚本默认是 `4`；本实验实际运行时按可用 GPU 数量和显存设置，例如单卡小模型可设为 `1`，多卡大模型可设为 `4`，并在 manifest/README 命令中记录清楚。
- 每条样本只暴露自己所属环境的工具 schema，不混合 A/B/C 或多个环境工具。
- `hard_no_tool` 不理解成“完全隐藏工具 schema”；它是 `no_tool` prompt 加 state machine 拒绝工具调用，用来测模型不靠工具能不能答对。
- 跑标签和构造数据：train/test 都做。
- 探测神经元和训练：只用 train。
- Base 评测、训练后评测和因果验证：只用 test。
- 抽激活阶段 prompt 必须复用 When2Tool 官方 `init_state(...)` 得到的 `messages/tools`，再渲染 chat template；不能手工拼 `system + user`，否则会漏掉 `ListManipulationEnv` 特殊 system prompt、`dialog` 字段样本和官方工具 schema 初始化细节。
- 抽激活阶段模型前向 dtype 默认使用 bfloat16；FFN last-token activation 正式实验保存为 float32，用于后续 SCAR 的均值、方差和 z-score 统计。smoke/debug run 可临时保存为 float16 以降低磁盘和 I/O 开销，但不能作为正式神经元发现结果。
- 生成式最终评测默认 `n_runs=3`，因为 When2Tool 的单跳/多跳主表是 3 次运行的 mean±std；跑标签、构造数据、探测和训练不按 3 次重复。

### 3.3 本实验按阶段怎么弄

| 顺序 | 阶段 | split | backend / prompt / mode | tool format | 输出 |
|---:|---|---|---|---|---|
| 1 | 跑标签 | train + test | `vllm` + `hard_no_tool` + `no_reasoning` + `enable_thinking=false`；`max_new_tokens=2048`、`max_rounds=12`、`max_model_len=32768`；`tensor_parallel_size` 按硬件设置并记录；`no_tool` prompt + 拒绝工具调用 | 自动检测；Llama 为 native，Qwen 为 XML | 每个样本的 `no_tool_correct` 和 `tool_necessary` |
| 2 | 构造改造后数据 | train + test | 不重新生成，只合并原始 When2Tool 字段、A/B/C 类型、模型专属标签 | 保留原样本自己的 env/tool schema | `task_type`、`tool_necessary`、原始 instruction/env/expected |
| 3 | 抽激活 | train + test | `current` + `no_reasoning` + `enable_thinking=false`；prompt 由官方 `init_state` 构造；system prompt 也按模型自动分流；前向 `bfloat16`，activation 正式保存 `float32`，smoke/debug 可临时保存 `float16` | Qwen/XML，Llama/native；影响 system prompt、chat template 和 tools schema | FFN target module 的 last-token activation |
| 4 | 探测 A/B/C 单类型神经元 | train | SCAR：`tool_necessary=1` vs `tool_necessary=0` | 不生成，不涉及工具调用格式 | `TDN_A/TDN_B/TDN_C` 和热力图 |
| 5 | 找跨类型共享神经元 | train | `TDN_A ∩ TDN_B ∩ TDN_C` | 不生成，不涉及工具调用格式 | `CTD`、pairwise/private neurons 和热力图 |
| 6 | 构造训练样本 | train | y=0 用 hard-no-tool 直接回答；y=1 用 `current/no_reasoning` 采样第一步合法工具调用；SFT tokenize 固定 `current/no_reasoning/enable_thinking=false` | Qwen 用 XML tool_call；Llama 用 native JSON tool call | CTD-Masked LoRA first-action 训练数据 |
| 7 | 训练 CTD-Masked LoRA | train | 只训练 CTD 坐标对应 masked LoRA update | 不改变工具格式 | LoRA adapter checkpoint |
| 8 | 训练后评测 | test | `current` + `no_reasoning` + `enable_thinking=false`；加载 CTD-Masked LoRA；不做 prefill | Qwen/XML，Llama/native | `CTD-Masked-LoRA` 绝对指标 |
| 9 | Base 模型评测与 delta 计算 | test | `current` + `no_reasoning` + `enable_thinking=false`；不加载 adapter；不做 activation mask；不做 prefill | Qwen/XML，Llama/native | `Base/Default` 绝对指标，以及 `CTD-Masked-LoRA` 相对 Base 的 delta 指标 |
| 10 | 因果验证 | test | `current` + `no_reasoning` + activation mask | Qwen/XML，Llama/native | `Mask-CTD` vs random/private 的 Acc/TC/TCR/ToolAcc |
| 11 | 引用 When2Tool baseline | paper table | 不复跑 Sparse / Reason-then-Act / Probe&Prefill；直接引用 When2Tool 论文表格 | 按论文原设置 | Sparse / Reason-then-Act / Probe&Prefill 对比数值 |

本实验会在相同模型、相同 test split、相同 prompt 设置下单独跑 `Base/Default`，用于和 `CTD-Masked-LoRA` 计算 delta 指标；暂不复现 Sparse / Reason-then-Act / Probe&Prefill，这三类结果直接引用 When2Tool 论文表格。

## 4. 标签和“平行数据对”

对模型 `m` 和样本 `x`，先跑 hard no-tool evaluation。定义：

$$
Correct_m^{NT}(x)=
\begin{cases}
1, & m \text{ 在无工具条件下答对 } x,\\
0, & m \text{ 在无工具条件下答错 } x.
\end{cases}
$$

工具必要性标签为：

$$
y_m(x)=1-Correct_m^{NT}(x)
$$

其中：

- `y_m(x)=0`：tool-unnecessary，这个模型不用工具也能答对。
- `y_m(x)=1`：tool-necessary，这个模型不用工具答不对，应该调用工具。

这里的 0/1 标签只来自模型自己的 hard no-tool 结果，和原始难度 `easy/medium/hard` 没有直接等价关系。`easy/medium/hard` 继续保留在 metadata 里，用来做分组统计和最终报告；但它不参与定义 `D^1/D^0`，也不作为神经元探测的标签。

设 `env(x)=e` 表示样本所属 When2Tool 环境，`g(e)\in\{A,B,C\}` 表示环境到任务类型的映射。先在环境内按模型标签分成 0/1：

$$
D_{m,s,e}^{1}=\{x\in D_s^{train}\mid env(x)=e,\ y_m(x)=1\}
$$

$$
D_{m,s,e}^{0}=\{x\in D_s^{train}\mid env(x)=e,\ y_m(x)=0\}
$$

再把属于同一类型的环境合并：

$$
D_{m,s,c}^{1}
=
\bigcup_{e:g(e)=c}D_{m,s,e}^{1}
$$

$$
D_{m,s,c}^{0}
=
\bigcup_{e:g(e)=c}D_{m,s,e}^{0}
$$

该类型的平行数据对定义为：

$$
\Pi_{m,s,c}=(D_{m,s,c}^{1},D_{m,s,c}^{0})
$$

说人话：A 类里，把模型“需要工具”的样本放一边，“不需要工具”的样本放另一边；B/C 也一样。它不是样本一一配对，而是 Safety Kernel 那种条件对比结构。

## 5. 神经元定义：直接套 Safety Kernel

Safety Kernel 把神经元定义在目标 MLP/FFN projection module 的输出坐标上。本实验同样如此。

对 Transformer 第 `l` 层的目标 FFN 线性模块 `q`，例如：

$$
q \in \{\texttt{mlp.gate\_proj},\texttt{mlp.up\_proj},\texttt{mlp.down\_proj}\}
$$

该模块输出为：

$$
a_{l,q}(x)\in \mathbb{R}^{T_x\times d_{l,q}}
$$

其中 `T_x` 是 prompt token 数，`d_{l,q}` 是模块输出维度。一个候选神经元定义为：

$$
n=(l,q,i),\quad i\in\{1,\dots,d_{l,q}\}
$$

为了对齐 When2Tool “生成前 last-token hidden state” 的设定，主实验使用最后一个输入 token 的模块输出坐标：

$$
h_n(x)=a_{l,q,t_{end},i}(x)
$$

也就是说，每个样本只取“模型看完 prompt、准备开始生成”这一刻的最后一个输入 token 激活。后续 SCAR 统计、热力图、交集和因果验证都统一使用这个位置，不再做全 token 平均或其他 token 聚合。

候选神经元全集为：

$$
\mathcal N_m=\{(l,q,i)\mid l\in Layers_m,\ q\in FFNModules_l,\ 1\le i\le d_{l,q}\}
$$

注意：这里不使用 Q/K/V/O attention neuron，也不做 attention projection 行/列置零；本实验按 Safety Kernel 版本使用 FFN 输出坐标。

## 6. 单类型神经元探测：SCAR 分数

对固定模型 `m`、子集 `s`、任务类型 `c`、神经元 `n=(l,q,i)`，把 `tool_necessary=1` 类比为 Safety Kernel 的 unsafe condition，把 `tool_necessary=0` 类比为 safe condition。

这一节是直接对齐 Safety Kernel 的发现步骤：Safety Kernel 是“每种语言单独算一遍 unsafe vs safe 的 SCAR 分数，再给每种语言选 top-k safety neurons”；这里改成“每个任务类型 A/B/C 单独算一遍 should-call-tool vs no-call-tool 的 SCAR 分数，再给每个类型选 top-k tool-decision neurons”。

所以 A、B、C 是分别探测，不是把三类混在一起探测：

$$
A:\ D_{m,s,A}^{1}\text{ vs }D_{m,s,A}^{0},\quad
B:\ D_{m,s,B}^{1}\text{ vs }D_{m,s,B}^{0},\quad
C:\ D_{m,s,C}^{1}\text{ vs }D_{m,s,C}^{0}
$$

说人话：A 类只用 A 类样本找 A 神经元，B 类只用 B 类样本找 B 神经元，C 类只用 C 类样本找 C 神经元。后面找共享神经元时，才把三类结果拿来取交集。

这里的 `D^1/D^0` 按跑出来的 `tool_necessary` 分，不按 `easy/medium/hard` 分。比如一个 hard 样本如果某个模型无工具答对了，它在这个模型的数据里就是 `D^0`；一个 easy 样本如果某个模型无工具答错了，它就是 `D^1`。难度只用于后续看结果在 easy/medium/hard 上分别怎样。

均值和标准差：

$$
\mu_n^{1}=\mathbb E_{x\in D_{m,s,c}^{1}}[h_n(x)],\quad
\sigma_n^{1}=\operatorname{Std}_{x\in D_{m,s,c}^{1}}[h_n(x)]
$$

$$
\mu_n^{0}=\mathbb E_{x\in D_{m,s,c}^{0}}[h_n(x)],\quad
\sigma_n^{0}=\operatorname{Std}_{x\in D_{m,s,c}^{0}}[h_n(x)]
$$

tool-necessary induced activation shift：

$$
\delta_n=\mu_n^{1}-\mu_n^{0}
$$

对同一模块 `(l,q)` 内的坐标做 module-wise z-score：

$$
z_{l,q}(r_i)=
\frac{r_i-\operatorname{Mean}_{j}(r_j)}
{\operatorname{Std}_{j}(r_j)+\epsilon}
$$

其中 `j` 遍历同一 `(l,q)` 模块里的全部输出坐标。

注意：这里的归一化只是为了消除不同层、不同 FFN 模块的激活尺度差异。它不是“每层单独选一批神经元”，也不是给每层固定名额。归一化后，每个神经元仍然只有一个可比较的 SCAR 标量分数，后面再把全模型候选集合放在一起排序。

pooled standard-deviation term：

$$
\rho_n=
\sqrt{
\frac{(n_1-1)(\sigma_n^1)^2+(n_0-1)(\sigma_n^0)^2}
{n_1+n_0-2}
}+\epsilon
$$

其中：

$$
n_1=|D_{m,s,c}^{1}|,\quad n_0=|D_{m,s,c}^{0}|
$$

Safety discriminability score：

$$
D_n=
z_{l,q}\left(
\frac{\delta_n}{\rho_n+\epsilon}
\right)
$$

这里按 Safety Kernel 论文公式字面对齐：`D_i=z_m(\Delta_i/(\rho_i+\epsilon))`，且 `\rho_i=\sqrt{\cdots}+\epsilon`。也就是 `\rho_n` 的定义里包含一个 `\epsilon`，`D_n` 的分母仍写成 `\rho_n+\epsilon`。`z_{l,q}` 自己分母里的 `\epsilon` 是 module-wise z-score 的数值稳定项，单独保留。

大白话：`D_n` 看的是这个神经元能不能把“该调工具”和“不该调工具”分开。差距越大、组内波动越小，分数越高。

Activation responsiveness score：

$$
R_n=z_{l,q}(\delta_n)
$$

大白话：`R_n` 看的是这个神经元在“该调工具”样本上有没有明显更激活。它不管组内方差，直接看激活差值在同模块里算不算突出。

最终 SCAR 分数：

$$
SCAR_n^{m,s,c}=D_n+R_n
$$

大白话：`SCAR` 同时奖励两件事：一是能稳定地区分 1/0 标签，二是对 `tool_necessary=1` 有明显响应。分数高的神经元更像“这个类型里判断是否该调工具”的关键坐标。

对每个类型单独选 top-k：

$$
TDN_{m,s,c}(k)=
\operatorname{TopK}_{n\in \mathcal N_m}
\left(SCAR_n^{m,s,c},k\right)
$$

主实验直接沿用 Safety Kernel 设置：

$$
k=5000
$$

这里的 `k=5000` 是每个模型、每个子集、每个任务类型在全模型候选集合里全局选 5000 个神经元，不是每一层 5000 个，也不是每个 FFN 模块 5000 个。也就是：

$$
|TDN_{m,s,A}|=5000,\quad |TDN_{m,s,B}|=5000,\quad |TDN_{m,s,C}|=5000
$$

这些 5000 个是从该模型所有候选 FFN 输出坐标 `\mathcal N_m` 里按 SCAR 排序选出来的。某些层可能选很多，某些层可能一个都没有。选完之后再按 layer/module 做热力图统计，所以热力图里的每层数量只是 top-5000 的自然分布结果。

得到：

$$
TDN_{m,s,A},\quad TDN_{m,s,B},\quad TDN_{m,s,C}
$$

这些就是 A/B/C 三类各自的“应该调工具”神经元。

## 7. 单类型神经元热力图

每个模型、每个子集都输出一张 A/B/C 单类型神经元分布热力图。

按层和模块统计数量：

$$
H_{m,s,c}(l,q)=
\left|\{i\mid (l,q,i)\in TDN_{m,s,c}\}\right|
$$

也报告归一化比例：

$$
\bar H_{m,s,c}(l,q)=
\frac{H_{m,s,c}(l,q)}{d_{l,q}}
$$

热力图画法：

- x 轴：FFN module，`gate_proj / up_proj / down_proj`。
- y 轴：layer index。
- 颜色：`\bar H_{m,s,c}(l,q)`。
- A/B/C 分三张子图，single-hop 和 multi-hop 分开。

这张图回答：A、B、C 的工具调用神经元分别集中在哪些层、哪些 FFN 模块。

除上面的 layer/module 分布密度图外，额外输出一组细粒度 `TDN-SCAR` 神经元图，用来直接查看“哪层、哪个模块、哪个神经元、SCAR 分数是多少”。这组图不参与神经元选择，只是对已经选出的 `TDN_{m,s,c}` 做可解释性展示。

对每个被选中的单类型神经元：

$$
(l,q,i)\in TDN_{m,s,c}
$$

保留它的：

$$
SCAR_i^{m,s,c},\quad rank_i^{m,s,c}
$$

细粒度图画法：

- x 轴：neuron index `i`，表示该 FFN module 输出向量里的具体坐标。
- y 轴：`layer + module`，例如 `L12.down_proj`。
- 颜色：该神经元的 `SCAR_i^{m,s,c}` 分数。
- 每个 subset 和每个类型 A/B/C 分开输出，避免 A/B/C 混在一张图里看不清。
- 图旁或配套表格列出 Top-N 神经元：`rank / layer / module / index / SCAR / discriminability / responsiveness / delta`。

因此单类型神经元探测阶段的可视化包含两层信息：密度热力图回答“集中在哪些层和模块”，`TDN-SCAR` 图回答“具体是哪几个神经元以及分数多高”。

## 8. 跨任务类型共享神经元

直接套 Safety Kernel 的“先各语言 top-k，再取交集”：

$$
CTD_{m,s}(k)=
TDN_{m,s,A}(k)
\cap
TDN_{m,s,B}(k)
\cap
TDN_{m,s,C}(k)
$$

其中交集按完整神经元身份 `(layer, module, index)` 精确匹配。

也保留三组 pairwise 交集，方便分析：

$$
P_{AB}=TDN_A\cap TDN_B,\quad
P_{AC}=TDN_A\cap TDN_C,\quad
P_{BC}=TDN_B\cap TDN_C
$$

共享率：

$$
ShareRate_{m,s,c}=
\frac{|CTD_{m,s}|}{|TDN_{m,s,c}|}
$$

共享神经元热力图：

$$
H^{CTD}_{m,s}(l,q)=
\left|\{i\mid(l,q,i)\in CTD_{m,s}\}\right|
$$

$$
\bar H^{CTD}_{m,s}(l,q)=
\frac{H^{CTD}_{m,s}(l,q)}{d_{l,q}}
$$

这张图回答：真正跨 A/B/C 共用的工具调用决策核心在哪里。

同样额外输出一组细粒度 `CTD-SCAR` 共享神经元图。因为 `CTD_{m,s}` 来自 A/B/C 三个 `TDN` 集合的交集，每个共享神经元保留三类任务里的 SCAR 分数：

$$
SCAR_i^{m,s,A},\quad SCAR_i^{m,s,B},\quad SCAR_i^{m,s,C}
$$

为了给共享神经元一个总体颜色值，默认报告：

$$
SCAR^{CTD\text{-}min}_i
=
\min(
SCAR_i^{m,s,A},
SCAR_i^{m,s,B},
SCAR_i^{m,s,C}
)
$$

也同时保存：

$$
SCAR^{CTD\text{-}mean}_i
=
\frac{
SCAR_i^{m,s,A}
+SCAR_i^{m,s,B}
+SCAR_i^{m,s,C}
}{3}
$$

默认用 `SCAR^{CTD-min}` 上色，因为 CTD 强调跨 A/B/C 都稳定重要，最小值能体现该共享神经元在最弱任务类型上的保底强度。

细粒度共享图画法：

- x 轴：neuron index `i`。
- y 轴：`layer + module`，例如 `L20.gate_proj`。
- 颜色：默认 `SCAR^{CTD-min}`，可额外输出 `SCAR^{CTD-mean}` 版本。
- 每个 subset 分开输出 single-hop 和 multi-hop。
- 配套表格列出 Top-N CTD 神经元：`layer / module / index / score_A / score_B / score_C / scar_min / scar_mean / rank_A / rank_B / rank_C`。

这样共享热力图不只说明 CTD 分布在哪些层和模块，也能直接定位最关键的共享神经元坐标及其跨类型 SCAR 强度。

## 9. CTD-Masked LoRA 训练

训练直接套 Safety Kernel 的 SK-Masked LoRA：冻结 backbone，只训练 LoRA；但 LoRA 的输出更新只能落在 CTD 对应 FFN 输出坐标上。

大白话理解：

- 前面探测已经找到了 `CTD_{m,s}`，也就是 A/B/C 都共用的一批“是否该调工具”的 FFN 输出坐标。
- 训练时不改整个大模型原参数，只在这些 FFN 线性层旁边加 LoRA 小分支。
- 普通 LoRA 会给整个 FFN 输出向量都加一个更新量；CTD-Masked LoRA 会把这个更新量乘上 mask，只允许 CTD 坐标被更新。
- 所以训练的目标不是让模型学一套全新的能力，而是把“该调工具时调工具、不该调工具时直接答”的行为，压到已经发现的共享工具调用神经元上。

可以把一个 FFN 输出向量想成：

```text
原始 FFN 输出: [v1, v2, v3, v4, v5, ...]
CTD mask:     [0,  1,  0,  1,  0,  ...]
LoRA 更新:    [a1, a2, a3, a4, a5, ...]
实际更新:     [0,  a2, 0,  a4, 0,  ...]
```

也就是说，只有 mask 为 1 的共享工具调用神经元会被 LoRA 训练影响；mask 为 0 的位置保持原模型输出。

### 9.1 训练数据

主实验是 A/B/C 三类一起训练。对固定模型 `m` 和子集 `s`，先构造每个任务类型自己的 first-action 训练样本集：

- 若 `y_m(x)=0`：使用 hard no-tool 条件下答对的直接回答作为第一步 assistant target。
- 若 `y_m(x)=1`：使用 `current/no_reasoning` 且工具可用时采样到的第一条合法 tool call 作为第一步 assistant target。

`y_m(x)=1` 不再保留完整工具轨迹，只保留第一步工具调用动作。写入训练集前只要求该动作能被 When2Tool parser 正确解析、工具属于当前任务 schema、工具执行 `success=True`；不要求工具后的最终答案正确。若多次采样仍得不到合法第一步 tool call，则从训练样本中剔除。

训练样本序列化必须和评测 prompt 对齐：

- SFT tokenize / chat template 固定使用 `current/no_reasoning/enable_thinking=false`。
- 每条样本只暴露自己所属 env 的 tool schema，不混合其他环境工具。
- Qwen 使用 XML system prompt，assistant 第一条工具调用是 `<tool_call>{...}</tool_call>`。
- Llama 使用 native tool calling chat template，assistant 第一条工具调用是 native bare JSON / tool call object。
- 对 `y_m(x)=0`，target 内容来自 hard-no-tool direct answer，但放到 `current/no_reasoning` 的工具可选 prompt 下训练；这样模型学到的是“工具可用时也可以不调用工具”。

单类型 first-action 训练样本集：

$$
R_{m,s,c}=
\{(x,\tau_x)\mid x\in D_{s,c}^{train}\},
\quad c\in\{A,B,C\}
$$

CTD-Masked LoRA 的主训练集是三类并集：

$$
R_{m,s}^{ABC}=
R_{m,s,A}\cup R_{m,s,B}\cup R_{m,s,C}
$$

其中 `\tau_x` 是第一步监督动作。loss 只算第一条 assistant target 的 token；system/user/tool schema 只作为上下文。

说人话：A/B/C 不分别训三个共享 LoRA，而是用同一个 `CTD_{m,s}` mask，在 A、B、C 的 train 样本上一起训练一个 LoRA。这样训练目标才对应“跨任务类型共享”的工具调用决策神经元。

### 9.2 Masked LoRA 公式

对目标 FFN 模块 `(l,q)`，原始线性层输出为：

$$
h_{l,q}=W_{l,q}u
$$

标准 LoRA 输出更新为：

$$
\Delta h_{l,q}
=
\frac{\alpha_L}{r}B_{l,q}A_{l,q}u
$$

CTD mask：

$$
M_{l,q,i}^{CTD}=
\begin{cases}
1, & (l,q,i)\in CTD_{m,s},\\
0, & \text{otherwise}.
\end{cases}
$$

CTD-Masked LoRA：

$$
\tilde h_{l,q}
=
W_{l,q}u
+
M_{l,q}^{CTD}\odot
\left(
\frac{\alpha_L}{r}B_{l,q}A_{l,q}u
\right)
$$

mask 作用在 LoRA output update 上，不改原始 `W_{l,q}u`。这点和 Safety Kernel 一致。

### 9.3 训练损失

训练采用 **First-Action Decision LoRA**。核心原则是：只监督第一步 assistant 动作，不把完整工具执行轨迹和工具后的最终答案放进 loss。这样训练目标更接近 When2Tool probe 的二分类思想，也能减少“训练数据里成功轨迹质量更好”带来的解释混淆。

对每个训练任务 `x`，标签仍然使用前面阶段得到的模型专属 `tool_necessary`：

$$
y_m(x)=
\begin{cases}
0, & \text{hard-no-tool 能答对，工具不必要},\\
1, & \text{hard-no-tool 答错，工具必要}.
\end{cases}
$$

训练样本统一放在 `current/no_reasoning/enable_thinking=false` 的工具可选 prompt 下。区别只在第一条 assistant target：

- 若 `y_m(x)=0`：target 是 assistant 直接给出 final answer，例如 `\boxed{...}`。这表示“工具可用，但此题不应该调用工具”。
- 若 `y_m(x)=1`：target 只保留 assistant 的第一条合法 tool call。训练样本不保留 tool response，也不保留看到 tool response 之后的 final answer。

`y_m(x)=1` 的 tool call 可以通过 `current/no_reasoning` 采样得到，但写入训练集前只检查第一步动作：

- 第一步输出能被 When2Tool parser 解析成工具调用。
- 调用的工具属于当前任务暴露的 tool schema。
- 工具执行结果 `success=True`。

不再要求这条采样轨迹的最终答案正确。也就是说，训练数据只教模型“该不该在第一步调用工具”，不教模型“调用工具后如何完成整道题”。说人话：我们不是用一批成功工具轨迹把模型整体教聪明，而是在 CTD 神经元上训练一个生成式二分类决策。

令第一步 assistant target 记为 `a_x^{(1)}`，其中：

```text
y=0: a_x^{(1)} = final answer tokens
y=1: a_x^{(1)} = first tool-call tokens
```

只对 `a_x^{(1)}` 内的 assistant token 计算 next-token cross entropy。system token、user token、tool schema token、tool response token，以及第一步之后的所有 token 都不参与 loss。

第一步 assistant token 集合记为 `A_1(x)`，训练损失为：

$$
\mathcal L_{decision}
=
-
\frac{1}{|R_{m,s}^{ABC}|}
\sum_{x\in R_{m,s}^{ABC}}
\frac{1}{|A_1(x)|}
\sum_{t\in A_1(x)}
\log p_\theta(a_{x,t}^{(1)}\mid x,a_{x,<t}^{(1)})
$$

其中每条样本先按自己的第一步 assistant token 数量平均，再对样本平均。这样可以避免 `y=1` 的 tool-call 序列或 `y=0` 的 final-answer 序列因为长短不同，在 loss 里天然占更大权重。

训练时：

$$
\theta_{base}\ \text{frozen},\quad
\{A_{l,q},B_{l,q}\}\ \text{trainable}
$$

主训练配置：

| 超参 | 值 |
|---|---:|
| LoRA rank `r` | 8 |
| LoRA alpha `alpha_L` | 16 |
| LoRA dropout | 0 |
| Epochs | 3 |
| Global batch size | 16 |
| Learning rate | `5e-5` |
| Warmup ratio | `0.03` |
| Optimizer | AdamW |
| Precision | bf16 |

single-hop 和 multi-hop 分别训练两个 CTD-Masked LoRA checkpoint。

## 10. 训练后评测

训练后只在 test split 上评测，并且 single-hop / multi-hop 分开报。本阶段只负责得到 `CTD-Masked-LoRA` 的绝对指标；相对 Base 的 delta 指标在下一节统一计算，避免混用不同阶段的 Base。

评测运行设置：

- `CTD-Masked-LoRA` 使用完全相同的 `current/no_reasoning/enable_thinking=false`，只额外加载训练好的 masked LoRA adapter。
- `CTD-Masked-LoRA` 不做 Probe&Prefill，不手动塞入 soft/hard prefill。
- Qwen 仍走 XML `<tool_call>...</tool_call>` / `<tool_response>...</tool_response>`；Llama 仍走 native JSON tool call + `role=tool` response。
- 本实验单独运行同口径 Base/Default，用于和 CTD-Masked-LoRA 计算 delta；暂不复现 Sparse / Reason-then-Act / Probe&Prefill，这三类结果直接引用 When2Tool 论文表格。
- 引用 baseline 时按论文原设置解释：single-hop Probe&Prefill 主对比为 `\tau=0.5`，Qwen 报 soft prefill，Llama 重点报 hard prefill；multi-hop Probe&Prefill 是 single-hop probe transfer 到 multi-hop。
- 本阶段保存逐样本评测结果和 overall/env/difficulty/A/B/C/y 分组绝对指标，供下一节读取并计算 delta。

开源依据：

- When2Tool README 的 baseline 命令就是 `prompt_mode=current` + `reasoning_mode=no_reasoning` + `n_runs=3`。
- README 把 `current` 标为 Default，把 `no_reasoning` 标为 Prompt-only baseline。
- `utils.py` 中 `current/no_reasoning` 的用户提示是“可以选择用工具或不用工具，并用 `\boxed{...}` 给最终答案”。
- `utils.py` 中 `detect_tool_format` 对 Llama 返回 native；Qwen 默认 XML。

### 10.1 对齐 When2Tool 的主指标

训练后主表直接对齐 When2Tool 开源聚合逻辑。最终答案正确性使用 `item_final_eval`：抽取最后 assistant response 里的 `\boxed{...}`，清洗后和 `expected.answer` 比较。工具调用数使用 `item_tool_calls`。expected steps 使用 `item_expected_steps`：single-hop 为 1，multi-hop 为 3。

主表报告：

- `Final Accuracy`
- `Total Tool Calls`
- `Avg Tool Calls`
- `Tool Call Rate`
- `Total Token Cost`
- `Avg Token Cost`

并且和 When2Tool 一样，报告 overall、按 `env`、按 `difficulty=easy/medium/hard` 的结果；多次生成评测时报告 mean±std。

准确率：

$$
Acc=
\frac{1}{N}\sum_{i=1}^{N}
\mathbb 1[\hat a_i=a_i^\star]
$$

总工具调用数：

$$
TC=\sum_{i=1}^{N}toolcalls_i
$$

平均工具调用：

$$
AvgTC=\frac{TC}{N}
$$

工具调用率：

$$
TCR=
\frac{
\sum_{i=1}^{N}toolcalls_i
}{
\sum_{i=1}^{N}expected\_steps_i
}
$$

其中 single-hop 的 `expected_steps=1`，multi-hop 的 `expected_steps=3`。

token cost：

$$
TotalTokenCost=
\sum_{i=1}^{N}token\_cost_i
$$

$$
AvgTokenCost=
\frac{TotalTokenCost}{N}
$$

相对 Base 的 delta、工具调用变化比例和 accuracy-cost-per-saved-call 不在本阶段单独计算，而是在下一节读取同口径 Base 评测结果后统一生成。

### 10.2 本实验额外统计

When2Tool 主指标能回答“最终答案准不准、工具少没少调”。但本实验还要证明 CTD-Masked LoRA 真的改善了“是否调工具”的决策，所以额外报告下面这些诊断指标。它们不替代 When2Tool 主指标，只用于解释机制。

定义测试样本上的实际工具调用决策：

$$
\hat y_i^{call}=
\mathbb 1[toolcalls_i>0]
$$

其中 `y_m(x_i)` 是该模型在 hard-no-tool 条件下跑出来的 `tool_necessary` 标签。

工具决策准确率：

$$
DecisionAcc=
\frac{1}{N}\sum_{i=1}^{N}
\mathbb 1[\hat y_i^{call}=y_m(x_i)]
$$

过度调用率：

$$
OverCall=
\Pr(\hat y^{call}=1\mid y_m(x)=0)
$$

漏调用率：

$$
UnderCall=
\Pr(\hat y^{call}=0\mid y_m(x)=1)
$$

工具调用 precision / recall / F1：

$$
Precision=
\frac{TP}{TP+FP+\epsilon},
\quad
Recall=
\frac{TP}{TP+FN+\epsilon}
$$

$$
F1=
\frac{2\cdot Precision\cdot Recall}
{Precision+Recall+\epsilon}
$$

其中正类是 `tool_necessary=1`，也就是“应该调工具”。

这些指标需要按下面维度分别统计：

- single-hop / multi-hop 分开。
- A/B/C 分开。
- `difficulty=easy/medium/hard` 分开。
- `y=0` 和 `y=1` 分开报告 final accuracy 与 avg tool calls。

另外保留工具格式诊断：

- valid tool-call rate：生成的 tool call 能否被 When2Tool parser 正确解析。
- invalid tool-call count：XML / native JSON 格式错误的次数。
- tool trajectory success rate：有工具调用的样本里，最终是否得到正确答案。

若统计 invalid tool-call count，需要保存足够的 raw output / trace；主评测指标仍按 When2Tool 默认聚合，格式诊断只作为额外分析。

Probe 指标只用于对齐 When2Tool 表格，不作为 CTD 发现的训练目标：

$$
AUROC=
\Pr(p(x^+)>p(x^-)),
\quad x^+\sim y=1,\ x^-\sim y=0
$$

## 11. Base 模型评测与 delta 指标

为了让 `CTD-Masked-LoRA` 的收益和代价可解释，正式实验需要在同一个 test split 上额外跑一次未训练、未干预的 base 模型，作为本实验内部的 `Default / Prompt-only` 参照点。这个 Base 评测只用于和阶段 10 的训练后评测结果计算 delta，不参与神经元发现、LoRA 训练，也不替代后面因果验证阶段里的 `Base` 对照。

关键原则：**训练后评测的 delta 必须来自同一模型、同一子集、同一 test split、同一分组、同一 prompt/tool/生成参数口径下的 Base 和 CTD-Masked-LoRA 两套结果。** 不用因果验证里的 `Base` 来和训练后评测混算，因为因果验证的 Base 是 activation mask 实验内部的参照，它的分组、运行次数和输出结构服务于干预比较。

### 11.1 Base 运行口径

Base 评测设置完全对齐 When2Tool 的 Prompt-only Default：

- split：只使用 `test`，single-hop 和 multi-hop 分开。
- prompt：`prompt_mode=current`。
- reasoning：`reasoning_mode=no_reasoning`，即 `require_reasoning=False`。
- thinking：`enable_thinking=false`。
- prefill：不使用 Probe&Prefill，不手动加入 soft/hard prefill。
- adapter：不加载 CTD-Masked LoRA，不加载任何训练 checkpoint。
- activation mask：不做任何神经元 mask。
- tool format：按模型自动分流，Qwen 使用 XML `<tool_call>...</tool_call>` / `<tool_response>...</tool_response>`，Llama 使用 native JSON tool call + `role=tool` response。
- tool schema 和状态机：仍通过 When2Tool 官方 `init_state(...)` / `evaluate_batched(...)` 构造和执行，每条样本只暴露自己所属 env 的 tool schema。

生成参数和 When2Tool `run_eval.py` 对齐：

| 参数 | 正式值 |
|---|---:|
| `n_runs` | 3 |
| `max_rounds` | 10 |
| `max_new_tokens` | 2048 |
| `max_model_len` | 32768 |
| `torch_dtype` / `vllm_dtype` | bfloat16 |
| `record_mode` | lite 或 full，正式表格至少需要能复算主指标和工具决策诊断 |

实现上，为了和 `CTD-Masked-LoRA` 训练后评测严格可比，Base 评测建议使用和阶段 10 相同的评测代码路径与生成后端，只是不传 adapter。原因是训练后模型必须加载自定义 CTD-Masked LoRA，无法直接使用官方 vLLM `AgentModel`；如果 Base 用 vLLM 而 CTD-Masked-LoRA 用 HF 后端，delta 会混入生成后端差异。这里的实现差异只限于加载/不加载 adapter 的工程入口，prompt、tool schema、parser、state transition 和评测指标仍然对齐 When2Tool 官方。

输出建议按模型和子集隔离：

```text
../cross_task_tool_neurons_data/outputs/<model_alias>/base_evaluation/<subset>/
|-- outputs.json
|-- per_task.jsonl
|-- summary.json
|-- summary_table.csv
|-- manifest.json
```

其中 `manifest.json` 必须记录 `model_alias`、`model_path`、`subset`、`n_runs`、`max_rounds`、`max_new_tokens`、`max_model_len`、`tool_format`、`prompt_mode=current`、`reasoning_mode=no_reasoning`、`enable_thinking=false`、`adapter=None`、`activation_mask=None` 和数据集 manifest 参数。已存在且 manifest 完全一致时提前跳过；参数不一致时必须重跑或显式清理。

### 11.2 Delta 计算

令 `b` 表示同口径 Base / Prompt-only Default：

$$
b=\text{Base Default}=(current,\ no\_reasoning,\ enable\_thinking=false,\ no\ adapter,\ no\ mask)
$$

令 `l` 表示 `CTD-Masked-LoRA`：

$$
l=\text{CTD-Masked-LoRA}=(current,\ no\_reasoning,\ enable\_thinking=false,\ adapter=CTD)
$$

对任意分组 `g`，例如 overall、A/B/C、env、difficulty、`tool_necessary=0/1`，先分别从 Base 评测和训练后评测中读取同名分组指标：

$$
M_{b,g},\quad M_{l,g}
$$

训练后对比表至少计算：

$$
\Delta Acc_{l,g}^{pp}
=
100\cdot(Acc_{l,g}-Acc_{b,g})
$$

$$
\Delta AvgTC_{l,g}
=
AvgTC_{l,g}-AvgTC_{b,g}
$$

其中 `\Delta Acc^{pp}` 用百分点报告。如果表里已经把 accuracy 写成 `88.2` 而不是 `0.882`，就不用再乘 100。

When2Tool 的 accuracy-cost-per-saved-call 使用：

$$
Cost_{l,g}
=
\frac{\Delta Acc_{l,g}^{pp}}
{-\Delta AvgTC_{l,g}},
\quad
\Delta AvgTC_{l,g}<0
$$

如果 `\Delta AvgTC_{l,g}\ge 0`，说明没有节省平均工具调用，`Cost` 记为 `NA`，不要硬算成有意义的 tradeoff。

另外报告两种工具调用变化比例，避免符号混淆。带符号的相对变化为：

$$
\Delta TC\%_{l,g}
:=
\frac{TC_{l,g}-TC_{b,g}}
{TC_{b,g}+\epsilon}
\times 100\%
$$

正向工具调用减少比例为：

$$
ToolCallReduction_{l,g}
=
\frac{TC_{b,g}-TC_{l,g}}
{TC_{b,g}+\epsilon}
\times 100\%
$$

`ToolCallReduction%=-\Delta TC%`。论文叙述里的“reduces tool calls by xx%”用正向 `ToolCallReduction%` 更直观；复刻 When2Tool 表格时用带符号的 `\Delta TC%`。在样本数相同的同一分组内，用 `TC` 或 `AvgTC` 算比例等价；为了和 multi-hop 表一致，主表用 `Total Tool Calls` 算。

本实验额外的工具决策诊断也可以计算 delta：

$$
\Delta DecisionAcc_{l,g}
=
DecisionAcc_{l,g}-DecisionAcc_{b,g}
$$

$$
\Delta OverCall_{l,g}
=
OverCall_{l,g}-OverCall_{b,g}
$$

$$
\Delta UnderCall_{l,g}
=
UnderCall_{l,g}-UnderCall_{b,g}
$$

这些 delta 只用于解释 CTD-Masked-LoRA 是否改善“该不该调工具”的决策，不替代 When2Tool 主指标。

最终生成的训练后对比表建议写到：

```text
../cross_task_tool_neurons_data/outputs/<model_alias>/trained_evaluation/<subset>/comparison_with_base.csv
```

这一表的每一行都必须包含 `model_alias`、`subset`、`group_kind`、`group_name`、`method=CTD-Masked-LoRA`、Base 绝对指标、CTD-Masked-LoRA 绝对指标以及上述 delta 指标。

## 12. 因果验证

因果验证放在 CTD-Masked LoRA 训练、Base 评测和训练后评测之后做。但是注意因果验证不是用训练后的模型做的，理论上这一步要在训练之前验证好，但是为了方便放到了最后。它在 test split 上运行，single-hop 和 multi-hop 分开。核心比较是：

1. `Base`：不干预。
2. `Mask-Random`：随机 mask，与 `CTD` 同数量、同层、同模块分布。
3. `Mask-TDN_c`：mask 单类型神经元。
4. `Mask-CTD`：mask 共享神经元。
5. `Mask-Private_c`：mask 单类型私有神经元。

单类型私有神经元定义为：

$$
Private_{m,s,c}=TDN_{m,s,c}\setminus CTD_{m,s}
$$

### 12.1 激活 mask

对神经元集合 `G`，在前向传播时把对应 FFN 输出坐标置零：

$$
\tilde a_{l,q,t,i}(x)=
\begin{cases}
0, & (l,q,i)\in G,\\
a_{l,q,t,i}(x), & (l,q,i)\notin G.
\end{cases}
$$

所有 token 位置都 mask，和 Safety Kernel 的 SK activation suppression 对齐。

### 12.2 工具行为指标

模型实际是否调用工具：

$$
u_{m,G}(x)=\mathbb 1[toolcalls_{m,G}(x)>0]
$$

工具决策准确率：

$$
ToolAcc_m(G,c)=
\frac{1}{|D_{m,s,c}^{test}|}
\sum_{x\in D_{m,s,c}^{test}}
\mathbb 1[u_{m,G}(x)=y_m(x)]
$$

必要工具召回：

$$
CallRecall_m(G,c)=
\frac{
\sum_x \mathbb 1[y_m(x)=1\land u_{m,G}(x)=1]
}{
\sum_x \mathbb 1[y_m(x)=1]
}
$$

不必要工具过调率：

$$
OverCall_m(G,c)=
\frac{
\sum_x \mathbb 1[y_m(x)=0\land u_{m,G}(x)=1]
}{
\sum_x \mathbb 1[y_m(x)=0]
}
$$

共享神经元的跨类型一致性：

$$
\Delta Acc_m(G)=
\frac{1}{3}\sum_{c\in\{A,B,C\}}
\left(Acc_m(G,c)-Acc_m(Base,c)\right)
$$

$$
\Delta TCR_m(G)=
\frac{1}{3}\sum_{c\in\{A,B,C\}}
\left(TCR_m(G,c)-TCR_m(Base,c)\right)
$$

$$
VarAcc_m(G)=
\operatorname{Var}_{c\in\{A,B,C\}}
\left(Acc_m(G,c)-Acc_m(Base,c)\right)
$$

如果 `Mask-CTD` 比 `Mask-Random` 更明显、更一致地改变 A/B/C 三类工具调用行为，就说明 CTD 不是统计交集，而是有因果作用的共享工具调用核心。

## 13. 最终报告表格

最终报告目录按模型标签隔离：

```text
outputs/final_report/<model_alias>/
```

跨模型汇总单独写到 `outputs/final_report/all_models/`，避免不同模型的表格和图片互相覆盖。

### 13.1 神经元发现表

| Model | Subset | Type | `|TDN_c|` | `|CTD|` | ShareRate | Top Layers | Top Modules |
|---|---|---|---:|---:|---:|---|---|

### 13.2 单类型因果验证表

| Model | Subset | Type | Intervention | Acc | AvgTC | TCR | ToolAcc | CallRecall | OverCall |
|---|---|---|---|---:|---:|---:|---:|---:|---:|

### 13.3 跨类型因果验证表

| Model | Subset | Intervention | Acc_A | Acc_B | Acc_C | AvgDeltaAcc | VarAcc | AvgDeltaTCR |
|---|---|---|---:|---:|---:|---:|---:|---:|

### 13.4 训练后对比表

| Model | Subset | Group | Method | Acc | TC | AvgTC | TCR | ΔAcc(pp) | ΔAvgTC | ΔTC% | TC Reduction% | Cost | ToolAcc |
|---|---|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|

`Method` 至少包括：

- `Base/Default`
- `CTD-Masked-LoRA`
- `Sparse`
- `Reason-then-Act`
- `Probe&Prefill`

`Group` 包括 overall、difficulty、env、A/B/C、`tool_necessary=0/1`。`ΔAcc(pp)`、`ΔAvgTC`、`ΔTC%`、`TC Reduction%`、`Cost` 都相对同组 `Base/Default=current/no_reasoning/enable_thinking=false` 计算。`Base/Default` 和 `CTD-Masked-LoRA` 是本实验在同一 test split、同一 prompt/tool/生成参数下实际运行得到的结果；Sparse / Reason-then-Act / Probe&Prefill 不复现，按 When2Tool 论文表格引用。注意：`CTD-Masked-LoRA` 本身不使用 Probe&Prefill，也不在推理时手动塞入工具调用前缀；它只加载训练好的 masked LoRA adapter，然后按 `current/no_reasoning` 正常生成。

### 13.5 神经元热力图产物归属

神经元热力图和神经元发现产物同阶段生成，不放到最终汇总阶段重新生成：

- 阶段 5 生成 `TDN-A/B/C` 单类型密度热力图，以及每个 `subset/type` 的细粒度 `TDN-SCAR` 神经元图。
- 阶段 6 生成 `CTD` 共享密度热力图，以及每个 `subset` 的细粒度 `CTD-SCAR` 共享神经元图。
- 最终报告阶段只汇总标签、神经元发现、共享神经元、训练、Base 评测、训练后评测和因果验证已有表格及最终结果图，不重新生成或复制神经元热力图。

细粒度 `TDN-SCAR` 图按照全局 TDN rank 展示已选 top 神经元：

- x 轴：全局神经元 rank。
- y 轴：`layer + module`，例如 `L12.down_proj`。
- 颜色：该神经元在对应任务类型中的 `SCAR_i^{m,s,c}`。

细粒度 `CTD-SCAR` 图保留共享神经元在 A/B/C 三类里的分数，并分别用下面两个排序量展示：

$$
SCAR_i^{CTD-min}=\min_{c\in\{A,B,C\}}SCAR_i^{m,s,c}
$$

$$
SCAR_i^{CTD-mean}=\frac{1}{3}\sum_{c\in\{A,B,C\}}SCAR_i^{m,s,c}
$$

这类图只用于报告和解释，不参与 SCAR 排序、交集计算、训练 mask 或因果干预定义。

## 14. 完整实验流程

对每个模型 `m`、每个子集 `s in {single-hop, multi-hop}`：

1. 跑 When2Tool hard no-tool，得到 train/test 的 `tool_necessary` 标签。
2. 按 A/B/C 和 0/1 标签构造平行数据对 `Pi_{m,s,c}`。
3. 在 train/test split 上提取 FFN target module 的 last-token activation；探测时只使用 train。
4. 在 train split 上对 A/B/C 分别计算 SCAR 分数，得到 `TDN_{m,s,A/B/C}`。
5. 输出 A/B/C 单类型神经元密度热力图和 `TDN-SCAR` 细粒度神经元图。
6. 取交集得到 `CTD_{m,s}`。
7. 输出 CTD 共享神经元密度热力图和 `CTD-SCAR` 细粒度共享神经元图。
8. 构造 CTD-Masked LoRA first-action 训练样本。
9. 用 train split 做 CTD-Masked LoRA 训练。
10. 在 test split 上评测训练后模型，指标对齐 When2Tool。
11. 在相同 test split、相同 prompt/tool/生成参数下评测未训练未干预的 Base/Default，并和训练后评测结果计算 delta 指标。
12. 在训练、Base 评测和训练后评测之后做因果验证：单类型 `Mask-TDN_c` vs `Mask-Random`，跨类型 `Mask-CTD` vs `Mask-Random` vs `Mask-Private_c`。

## 15. 预期写法

如果实验成立，结论可以这样写：

1. When2Tool 的工具必要性不只在线性 probe 中可解码，还能定位到稀疏的 FFN 输出坐标。
2. A/B/C 三类任务分别存在单类型工具调用决策神经元。
3. 三类单类型神经元的交集 `CTD` 是跨任务类型共享的工具调用决策核心。
4. 只通过 CTD-Masked LoRA 更新共享神经元，可以学习更好的工具调用校准，并能直接和同口径 Base/Default 计算 delta，同时和 When2Tool 的 Sparse、Reason-then-Act、Probe&Prefill 论文表格做外部参照。
5. 在训练和评测之后做因果验证，若 mask `CTD` 比同规模 random mask 更一致地影响 A/B/C 的工具调用行为，就说明 CTD 不是统计交集，而是有功能作用的共享核心。

## 16. 参考

- Safety Kernel / Who Transfers Safety?: https://arxiv.org/abs/2602.01283
- When2Tool paper: https://arxiv.org/abs/2605.09252
- When2Tool arXiv: https://arxiv.org/abs/2605.09252
- When2Tool code: https://github.com/Trustworthy-ML-Lab/when2tool
- When2Tool dataset: https://huggingface.co/datasets/cesun/When2Tool
  
